import styled from 'styled-components'

export const Loader = styled.span`
  font-size: 0.875rem;
  line-height: 1.25rem;
  font-weight: 600;
  color: var(--color-primary);
  margin-top: 4px;
  display: block;
`
